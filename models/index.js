// Import the User module from the specified file './User'
const User = require('./User');

// Import the Thought module from the specified file './Thought'
const Thought = require('./Thought');

// Export the User and Thought modules as properties of the exported object
module.exports = { User, Thought };
